days = ('mon', 'tue', 'wed', 'mon')
print(days[1])


# we cannot update the value of the tuple but can overwrite the tuple means days can be again written
days = (12, 90)
print(days)
# count the number of values
print(days.count('mon'))

